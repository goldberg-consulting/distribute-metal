model:
  hidden_dim: 256
  num_layers: 4
  num_classes: 10

training:
  batch_size: 128
  epochs: 10
  learning_rate: 0.001
  weight_decay: 0.01
  log_interval: 10

data:
  input_dim: 784
  num_samples: 10000
